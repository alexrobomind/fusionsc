stages:
  - build
  - test
  - pybuild
  - doc
  - pages

# Build stage

.jtpl: &job
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main"'
    - when: manual

.build: &build
  <<: *job
  stage: build
  artifacts:
    paths:
    - build/

.buildl: &build-linux
  <<: *build
  script:
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build . --target tests

.buildw: &build-windows
  <<: *build
  script:
    - mkdir build
    - cd build
    - cmake -A x64 ..
    - cmake --build . --target tests

.test: &test
  <<: *job
  stage: test
  script:
    - cd build
    - ctest . --no-tests=error
  artifacts:
    paths:
      - build/tests/*.xml
    reports:
      junit: 
        - build/tests/*.xml


.linux: &linux
  before_script:
    - apt-get update --yes
    - apt-get install --yes cmake python3 python3-pip libssl-dev

.msvc: &msvc
  tags:
    - msvc2017
  
  # Currently we only have windows builds available only for protected branches
  rules:
    - if: '$CI_COMMIT_REF_PROTECTED == "true" && $CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_REF_PROTECTED == "true" && $CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_REF_PROTECTED == "true"'
      when: manual
    - when: never

.gcc: &gcc
  <<: *linux
  image: gcc

.clang: &clang
  <<: *linux
  image: debian:bookworm
  before_script:
    - apt-get install --yes clang-14

build:gcc:
  <<: *build-linux
  <<: *gcc

build:clang:
  <<: *build-linux
  <<: *clang

build:msvc:
  <<: *build-windows
  <<: *msvc

# Test stage

test:gcc: 
  <<: *test
  <<: *gcc
  needs:
    - build:gcc

test:clang: 
  <<: *test
  <<: *clang
  needs:
    - build:clang

test:msvc: 
  <<: *test
  <<: *msvc
  needs:
    - build:msvc
    
# Python build

pybuild:pybuild-linux:
  <<: *job
  
  stage: pybuild
  needs: []
  
  image: quay.io/pypa/manylinux2014_x86_64
  parallel:
    matrix:
      - PYTHON: [cp37-cp37, cp38-cp38]
  
  before_script:
    - /opt/python/$PYTHON/bin/pip install build
    #- yum install -y perl-IPC-Cmd
    #
    #- mkdir _openssl
    #- cd _openssl
    #- git clone git://git.openssl.org/openssl.git
    #- cd openssl
    #- ./Configure no-shared
    #- make -j 8
    #- make install
    #- cd ../..
    #- rm -r _openssl
    - yum install -y openssl-static
  script:
    #- export OPENSSL_ROOT_DIR=/usr/local/ssl
    #- echo $OPENSSL_ROOT_DIR
    #- ls -R $OPENSSL_ROOT_DIR
    - /opt/python/$PYTHON/bin/python -m build
  

# Documentation (stands separate from the compilation-related jobs)

doc:
  <<: *job
  stage: doc
  image: gcc

  needs: []

  script:
    - apt-get update --yes
    - apt-get install --yes doxygen graphviz
    - doxygen Doxyfile
  
  artifacts:
    paths:
      - gendocs

pages:
  <<: *clang
  <<: *job
  stage: pages
  
  needs: []
  
  script:
  - pip3 install sphinx myst_nb sphinx_rtd_theme numpy
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build . --target docs
  - cd ..
  - mkdir public
  - cp -R build/docs/sphinx_docs public
  
  artifacts:
    paths:
      - public